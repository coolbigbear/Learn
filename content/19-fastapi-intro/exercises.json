[
  {
    "slug": "fastapi-hello",
    "title": "Hello World FastAPI",
    "instruction": "Create a FastAPI app with a single GET endpoint at the root `/` that returns `{\"message\": \"Hello, FastAPI!\"}`.",
    "starter_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n# Your code here\n",
    "solution_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n@app.get(\"/\")\nasync def root():\n    return {\"message\": \"Hello, FastAPI!\"}\n",
    "test_cases": [
      {
        "input": "",
        "expected_output": "",
        "comparison_type": "exact"
      }
    ],
    "order": 1
  },
  {
    "slug": "fastapi-echo-param",
    "title": "Echo a path parameter",
    "instruction": "Create a GET endpoint at `/echo/{message}` that returns `{\"you_said\": \"<message>\"}` where `<message>` is the path parameter.",
    "starter_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n# Your code here\n",
    "solution_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n@app.get(\"/echo/{message}\")\nasync def echo(message: str):\n    return {\"you_said\": message}\n",
    "test_cases": [
      {
        "input": "",
        "expected_output": "",
        "comparison_type": "exact"
      }
    ],
    "order": 2
  },
  {
    "slug": "fastapi-greet-query",
    "title": "Greet with query parameter",
    "instruction": "Create a GET endpoint at `/greet` that accepts an optional query parameter `name` (default: \"World\") and returns `{\"greeting\": \"Hello, {name}!\"}`.",
    "starter_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n# Your code here\n",
    "solution_code": "from fastapi import FastAPI\n\napp = FastAPI()\n\n@app.get(\"/greet\")\nasync def greet(name: str = \"World\"):\n    return {\"greeting\": f\"Hello, {name}!\"}\n",
    "test_cases": [
      {
        "input": "",
        "expected_output": "",
        "comparison_type": "exact"
      }
    ],
    "order": 3
  }
]